services:
  leads:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
      CONFIG_PATH: /app/config.yaml
    volumes:
      - ./data:/app/data
